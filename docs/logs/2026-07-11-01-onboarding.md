# 2026-07-11: 初回オンボーディング(評価・環境構築・Skill実装・レビュー)

このリポジトリでの最初のセッション。初回スキル評価から、育成リポジトリの構築、`/start` Skillの実装、全体レビューまでを1日(1つの継続した会話)の中で実施した。

## 初回スキル評価

- 種別: 会話・評価(設計課題+トラブルシューティング)
- 内容:
  - 背景ヒアリング(経歴、AWSハンズオン範囲、IaC経験、開発経験)
  - 設計課題: 会員制ECサイトのアーキテクチャ設計(VPC構成、各層のサービス選定、SG/NACL設計、可用性、DB選定)
  - トラブルシューティング課題: ECS Fargateタスクが S3 AccessDenied で失敗するシナリオの原因切り分け
- 発見:
  - 良かった点: VPCの3層分離、SGの設計思想(SG参照によるルール設計)、マネージドサービス選好の判断軸、S3のARN粒度、症状ベースの障害切り分け思考は同年次の中でも精度が高い
  - 改善が必要な点: NACLのステートレス挙動、RDS Multi-AZとリードレプリカの目的の違い、ECSのTask Role/Execution Roleの違い、いずれも「似た機能の目的差分」を混同するパターン
  - Terraformは経験ほぼゼロ
- CURRENT_LEVELへの反映: [CURRENT_LEVEL.md](../CURRENT_LEVEL.md) を新規作成、この評価結果を反映済み

## 育成リポジトリの構築

- 種別: 環境構築
- 内容:
  - docs/(PROFILE, CURRENT_LEVEL, LEARNING_LOG, ROADMAP, MENTOR_RULES)、projects/、templates/ の構成を新規作成
  - 初回評価の結果をPROFILE.md / CURRENT_LEVEL.mdに反映
  - 本人からのヒアリングでapply方針(plan中心・要所のみapply・apply後destroy)、予算感(月数千円)、Git管理の可否(git init可)を確認し、PROFILE.md / MENTOR_RULES.md / .gitignore に反映
  - 全ファイルをレビューし、「詰まった」の判断基準、構文と設計判断の切り分けルール、コスト・安全のガードレール、Terraform構文導入ステップ(`projects/00-terraform-intro`)を追加
  - ルートREADME.md、.gitignoreを新規作成し、`git init`を実行(ローカルのみ、コミットはまだ)
- 発見:
  - `.skills/`という仮の置き場所を作ったが、Claude Codeがプロジェクト固有のSkillを認識する正式な場所ではないと判明(正しくは`.claude/agents/`と同じ規約の`.claude/skills/<name>/SKILL.md`)。中身のREADME.mdもROADMAP/LEARNING_LOGの持ち越し事項と重複していたため削除した
  - `templates/readme-template.md`の「苦労した点」「学んだこと」は`RETROSPECTIVE.md`と役割が重複(特に「学んだこと」はレビュー前に書くには時期尚早)と判断し削除。「わからなかったこと・迷ったこと」はレビュー前のリアルタイムな理解度シグナルとして価値があるため残した

## `/start` Skillの設計・実装

- 種別: 環境構築(Skill実装)
- 内容:
  - `.claude/skills/start/SKILL.md` を新規作成。状態判定(`projects/README.md` の状態列を正本とする)とフェーズ遷移(出題→設計レビュー→AWS構築サポート→Terraformレビュー→振り返り・記録更新)のロジックを実装。教え方・レビュー観点は複製せず、毎回 `docs/MENTOR_RULES.md` を参照する形にした
  - `projects/README.md` に状態列の値の一覧を追加
  - `docs/ROADMAP.md` に、`01`以降は「設計レビュー→AWSコンソール構築→Terraform化」の順で進めること、`00`は構文導入のため設計/AWS構築フェーズを飛ばす例外であることを追記

## リポジトリ全体の厳格レビュー

- 種別: 環境構築(レビュー・修正)
- 内容: 全ファイルを突き合わせ、以下を修正
  - `projects/README.md`の命名例が`docs/ROADMAP.md`の実際のお題名(`01-vpc-network`)と食い違っていたのを修正
  - `.claude/skills/start/SKILL.md`の「全フェーズ共通のルール」がMENTOR_RULES.md/PROFILE.mdの内容を複製していたため、参照のみに変更(このSkill自身が掲げた「複製しない」原則に反していたのを是正)
  - SKILL.mdのAWS構築フェーズ(フェーズC)にコスト・安全ガードレールへの参照が抜けていたため追加
  - SKILL.mdに、状態列が想定外の値だった場合はユーザーに確認するフォールバックを追加
  - `docs/PROFILE.md`のレビュー観点リストがMENTOR_RULES.mdと全文重複していたため要約し、詳細はMENTOR_RULES.mdを参照する形に変更
  - `templates/design-review-template.md`にREVIEW.mdが複数回のレビューを追記していくファイルである旨を明記
  - `templates/readme-template.md`の実装欄をAWSコンソール構築記録とTerraform実装に分割
  - ルート`README.md`の読む順番に`projects/README.md`を追加

## LEARNING_LOGの構造変更、および粒度の見直し

- 種別: 環境構築(ドキュメント構成変更)
- 内容:
  - 単一ファイルの `docs/LEARNING_LOG.md` が長期運用で肥大化する懸念から、`docs/logs/` にセッション単位でファイルを分割し、`docs/LEARNING_LOG.md` は索引(1セッション1行、リンクのみ)に変更した
  - 「課題ごと」ではなく「セッションごと」に分割した理由: `projects/NN-お題名/RETROSPECTIVE.md` と役割が重複するため。セッションログ=時系列の作業記録、RETROSPECTIVE.md=お題ごとの理解度振り返り、と役割分担を明確化した
  - 初回実装時、今日1日分の作業を話題ごとに5ファイルへ分割してしまい、本人から「今回のここまでのログは1つでよい」とフィードバックを受けた。**セッションの単位は「話題」ではなく「1回の継続した作業のまとまり」であるべき**と学び、このファイル自体を1ファイルに統合した
- 発見: ログの分割粒度を誤ると、索引を見ただけでは全体像が逆に追いにくくなる(細切れになりすぎる)。分割は主に「日をまたぐ/大きく間が空く」タイミングで行うのが妥当
- CURRENT_LEVELへの反映: なし

## 最終レビュー(2回目)

- 種別: 環境構築(レビュー・修正)
- 内容: コミット前の最終確認として、もう一段細かく全ファイルを突き合わせ、以下を修正
  - `templates/readme-template.md`の「apply有無」欄がTerraform用語のままで、AWSコンソールで直接構築した課金対象リソースの状態を表せていなかった。「AWSリソースの状態(コンソール構築分)」と「Terraformの状態」の2項目に分割し、コスト事故のリスクを減らした
  - `.claude/skills/start/SKILL.md`フェーズAで、`projects/README.md`に行を追加する際「状態」しか埋める指示がなかったため、お題名・種別も記入する旨を明記
  - `.claude/skills/start/SKILL.md`フェーズCで、AWSコンソール構築後にREADME.mdの「AWSコンソールでの構築記録」を埋めるよう促す一文が抜けていた(テンプレートに欄だけあってSkillが使っていなかった)ため追加
- 発見: 特になし
- CURRENT_LEVELへの反映: なし

## 最終レビュー(3回目): SKILL.mdの実行トレース

- 種別: 環境構築(レビュー・修正)
- 内容:
  - `git status`/`git check-ignore`/`git add -An`を実際に実行し、`.gitignore`の除外設定と追跡対象ファイルが意図通りであることを検証。副産物として`.gitignore`だけが原因不明でステージ済みになっている状態を発見(実害はないが本人に報告)
  - `.claude/skills/start/SKILL.md`を実際に頭の中で実行トレースし、`projects/00-terraform-intro`(設計・AWS構築フェーズを飛ばす例外お題)を通したときに矛盾する箇所を2件発見:
    - フェーズAの締めが無条件に「まず設計から始めてほしい」と伝える指示になっており、設計フェーズが存在しない例外お題では誤った案内になる → 条件分岐を追加
    - フェーズDの書き出しが「AWSコンソールで構築できた構成を、同じものとして実装」を前提にしており、フェーズB・Cを経ていない例外お題には再現対象が存在しない → 条件分岐を追加
  - フェーズEに、今回新たに判明した弱点がROADMAP.mdにまだ無ければ項目を追加する旨を明記(従来はチェックと優先順位調整のみで、新規追加の権限が曖昧だった)
- 発見: 状態遷移ロジックは「通常フロー」を書いてから例外を後付けすると、本文の各所に通常フロー前提の文言が紛れ込みやすい。次にフローを追加・変更するときは、例外ケースを都度通しでトレースする
- CURRENT_LEVELへの反映: なし
- 次回への持ち越し: 初回コミット、`projects/00-terraform-intro`から学習サイクルを`/start`経由で開始する
