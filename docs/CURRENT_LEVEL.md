# CURRENT_LEVEL

学習者の現在の理解度。**このファイルは生きたドキュメント。** 新しい課題のレビュー結果が出るたびに更新すること(古い情報を消すのではなく、変化がわかるように更新日と根拠を残す)。

最終更新: 2026-07-27(`06-multi-az-failure-simulation`振り返り反映)

## 評価方法(初回)

会話 + 設計課題(会員制ECサイトのアーキテクチャ設計) + トラブルシューティング課題(ECS/S3のAccessDenied)。AIやネット検索を使わず本人の知識のみで回答してもらった。

## 理解できていること

| 項目 | 根拠 |
|---|---|
| VPCの3層ネットワーク分離、パブリック/プライベートの使い分け | 設計課題で理由付きに正しく回答できた |
| セキュリティグループの挙動(ステートフル、SG参照によるルール設計) | 「静的IPでなくSG参照にすべき」と自発的に言及。チュートリアル止まりでは出ない知識 |
| 運用負荷を踏まえたマネージドサービス選好の判断軸 | 「3人チームで運用兼任」からサーバーレス/コンテナ優先の発想を導けた |
| RDS/DynamoDBの使い分けの基準(データ構造で判断) | 妥当な理由付けができた |
| S3のARN粒度・キー構造の正確な理解 | バケット/*の区別、S3はフラットでフォルダ概念がない、まで正確に説明 |
| 症状からレイヤーを絞り込む障害切り分けの思考プロセス | 「AccessDeniedが出ている=リクエストはS3に届いている」と正しく推論。`00-terraform-intro`での`terraform apply`長時間ハングの原因究明でも、複数の仮説をCloudTrail・aws-cliでの実地検証を使って検証・棄却しながら自力で正解にたどり着けた(2026-07-11) |
| Terraformの`init`/`plan`/`apply`/`destroy`それぞれの役割 | `00-terraform-intro`で一連の流れを実行し、何も見ずに正確に説明できた(2026-07-11) |
| NAT GatewayとIGWの役割分担、AZごとに分離する理由 | `01-vpc-network`の設計レビュー再提出で、可用性とコストのトレードオフを自分の言葉で説明できた。理解度テストでも「NAT Gatewayは戻りの通信のみ許可、IGWは新規のインバウンド接続も許可する」という双方向性の違いまで正確に整理できた(2026-07-12, 2026-07-13) |
| EIP(固定パブリックIP)がNAT Gatewayに必要な理由 | 「外部サービスのIP許可リストに登録するため、静的かつ`/32`で絞れる固定IPが必要」と正確に説明できた(2026-07-13) |
| ALBのリスナー/ターゲットグループ/リスナールールの役割分担 | AWSコンソール構築後の理解度テストで正確に説明できた(2026-07-12) |
| ECSクラスターの役割(キャパシティプロバイダーを束ねる論理的名前空間) | 前回「わからない」と回答していたが、`01-vpc-network`のTerraform実装時に定義を再確認した上で、「1クラスターに複数サービスを乗せるのは自然」と正しく判断できた(2026-07-13) |
| ECS Service Auto Scalingが増減させるのは「タスクの数」であること(タスクとコンテナの関係) | Terraform実装時に一度「コンテナ数を増減させる」と誤解したが、対話を通じて「1タスク=1ENI=1プライベートIP、タスク定義内の複数コンテナは役割分担であり複製ではない」と正しく整理できた(2026-07-13) |
| RDS Multi-AZの目的(高可用性・自動フェイルオーバー) | 理解度テストで「プライマリがダウンしたら自動でフェイルオーバーし、ダウンタイムを最小に抑える。同期的にデータ同期している」と正確に説明できた(2026-07-13)。ただしリードレプリカ(スケーラビリティ目的)との対比は`03-rds-scaling`で未検証のため、下記「曖昧なこと」に別項目として残す |
| tfstateと変数の`sensitive`属性の違い | 「`sensitive`はCLI出力を隠すだけで、tfstate自体には平文で残る。だからGit管理は別問題として避けるべき」と正確に区別できた。以前「実感が伴わない」とされていた項目の解消(2026-07-13) |
| NACLのステートレス挙動とエフェメラルポートの扱い | `02-nacl-experiment`で、実験設計・実機での壊す/直すの検証・記録を見ない理解度チェックのすべてで一貫して正確に説明できた。「戻り通信にもOutboundの明示的な許可が必要」「エフェメラルポートは宛先ポートとして評価される」まで正確(2026-07-13) |
| SGとNACLの許可/拒否モデルの違い(SGは許可ルールのみ追加可能、NACLは許可/拒否を明示的に指定) | `02-nacl-experiment`で、SGに「Deny」の概念がないことに自力で気づき、記録を見ない理解度チェックでもコンソール/Terraformそれぞれの挙動の違いまで正確に説明できた。「似た機能の目的差分の混同」パターンを自力で乗り越えた事例(2026-07-13) |
| SGのステートフル性が効く範囲の境界(許可されたインバウンドへの応答には効くが、インスタンスからの新規発信トラフィックには効かない) | `02-nacl-experiment`のTerraform実装で、Egressルールがないと`yum install`が失敗することを自ら経験し、記録を見ない理解度チェックでもその実体験と結びつけて正確に説明できた(2026-07-13) |
| RDS Multi-AZとリードレプリカの目的差分(可用性 vs スケーラビリティ) | `03-rds-scaling`のAWSコンソール構築後の理解度チェック(10問中8問正解)で、書き込み拒否・エンドポイント分離の理由・フェイルオーバー時のエンドポイント不変などを正確に説明できた(2026-07-14)。設計レビュー・Terraform実装でも役割分担を一貫して正しく扱えた。「ほぼ理解できていないこと」から格上げ |
| `for_each`を使うべきかどうかの判断基準(値の一致でなく役割の一致で判断) | `03-rds-scaling`で、当初「値(cidr/az等)が一致するか」で誤って判断したが、SGルールとの比較・ルートテーブルでの過去の判断との比較という2つの参照点を示された上で、自力で正しい基準にたどり着いた(2026-07-16) |
| SGの`from_port`/`to_port`の意味(送信元ポートの概念はなく、宛先ポートの範囲のみを表す) | `03-rds-scaling`で、NACLとの対比(ステートフル/ステートレスの違い)から対話を通じて正しく理解し直せた(2026-07-16) |
| ポート範囲の数値的包含関係の理解と、それを用いた批判的検証 | `03-rds-scaling`で、「NACL Outboundに3306専用ルールが必要」というメンター側の誤った指摘に対し、「1024-65535が3306を数値的に含む」という事実と実機での動作確認結果の両方から自力で反論し、誤りを正した(2026-07-16)。振り返り後の記録を見ない理解度チェックでも、「rule_actionがdenyなら逆に拒否される」「プロトコル(TCP/UDP)の一致確認も必要」という点まで、聞かれていない部分に自発的に言及して正確に説明できた |
| RDS Multi-AZの手動フェイルオーバー時にエンドポイントのホスト名が維持される仕組み(DNSレコードの参照先切り替え) | AWSコンソールでの構築時(2026-07-14)の理解度チェックでは「MACアドレステーブルの切り替え」と誤って推測していたが、2日後の`03-rds-scaling`振り返り後の記録を見ない理解度チェック(2026-07-16)で「DNSによってエンドポイントへのリクエスト先をプライマリからスタンバイに変更している」と正確に説明できた。日をまたいだ定着が確認できた事例 |
| リードレプリカのレプリケーションが非同期である理由(同期にするとプライマリの応答がレプリカの処理完了を待つことになり、負荷分散という目的に矛盾するため) | 2026-07-14の構築時理解度チェックでは「CPU効率のため」と誤答していたが、2日後の2026-07-16の記録を見ない理解度チェックで正しい理由を再現できた。日をまたいだ定着が確認できた事例 |
| SGの自己参照ルールが、プライマリ⇄リードレプリカ間の新規接続をカバーする構造(送信側にEgress自己参照、受信側にIngress自己参照が必要) | 2026-07-14の構築時は「ステートフル性の説明はできるが、Inbound/Outbound自己参照それぞれの役割分担の説明は半分に留まった」状態だったが、2026-07-16の記録を見ない理解度チェックで「リードレプリカ発信なのでアウトバウンドの許可が必要、プライマリにはインバウンドルールが必要」と、送信側/受信側の役割分担まで正確に説明できた |
| ECS Task Execution Role/Task Roleの具体的な担当分担(特にCloudWatch Logsへの書き込み) | `01-vpc-network`で2026-07-12、2026-07-13と2回連続誤答していた論点。`04-ecs-iam-roles`では、管理ポリシーを使わず自力でAPIアクションを洗い出し、CloudWatch Logsの書き込みをExecution Role側に迷わず割り当てられた。さらに、Task Roleの権限を意図的にDenyにした実機テストで、CloudWatch Logsのエラーメッセージ(ARN・「explicit deny」の文言)だけからヒントなしで原因(Task Role)を正確に切り分けられた(2026-07-19)。ただし振り返り後の記録を見ない理解度チェックでは、結論(Execution Role)は即答できたものの、理由の説明で最初「起動時/起動後」という誤った軸に戻ってしまい、「誰が実行するか(エージェント自身かアプリコードか)」の軸を再度示すと即座に言い直せた。実践(設計・構築)では定着しているが、理由付けの言語化は完全に自動化されていない |
| `for_each`の判断基準(役割ベース)の新しい題材への自力転用、および`variable`/`locals`の使い分け(外部からの値注入の有無で判断) | `05-ecs-autoscaling`で、ALBサブネットに対して指摘される前に自分から「配置目的が同じだから`for_each`にすべき」と提起し、NAT/ECSサブネットは役割が異なるため対象外と正しく判断できた。`variable`/`locals`の使い分けも一度の説明で理解し、以後は自発的にIAMトラストポリシーの共通化を提案・実装した(2026-07-23)。`for_each`のループ変数(`each.value.xxx`)を使ったリソース定義も、教わらずに自力で正しく書けていた。`02`〜`04`で継続フォロー中だった項目の実践的な解消 |
| ECS Agentの役割(イメージプル・ログ送信の準備)と、タスクのENI・SGの関係の整理 | `05-ecs-autoscaling`で、`04-ecs-iam-roles`で学んだTask Execution Roleの知識(「誰が実行するか」の軸)を、ECS Agentという新しい切り口の質問に自分で結びつけ、SGがタスクのENIに紐づく理由を正しく整理できた(2026-07-23) |
| ALB・ECSサービス・ECSエージェント・Application Auto Scaling・CloudWatchアラームの役割分担 | `06-multi-az-failure-simulation`の設計レビュー(2026-07-25)で、当初ALBとECSサービスの役割(特に「異常なタスクを止める」の主語)を繰り返し混同したが、記録を見ない理解度チェックを2巡経て「2つの独立した入力が同じECSサービスの調整ループに合流する」という統合的理解に到達。Terraform実装後(2026-07-27)の記録を見ない理解度チェックでも、ECS側の役割(タスク配置の継続試行・ターゲットグループへの登録解除の決定)を実機ログを根拠に正確に再現でき、日をまたいだ定着を確認した(ALB側は下記「曖昧なこと」参照) |
| AZ障害を実機で意図的に再現し、事前予測と実際の挙動を比較する実験の設計・実施能力 | `06-multi-az-failure-simulation`で、NACL/SG/ルートテーブル破壊という複数の障害注入手段を比較検討した上でNACLを選定し、実験手順の設計、事前予測とAWS CLIでの実機観察の比較までを一貫して自走できた(2026-07-25設計レビュー、2026-07-27実装・観察) |
| ECSのAZ Rebalancing機能(`availability_zone_rebalancing`)の存在と、無効時に一度崩れたAZ間タスクバランスが自己修復されない理由 | `06-multi-az-failure-simulation`のTerraform実装後(2026-07-27)、AWSコンソール構築時に「原因未解明」だった「AZタスク配置が2:2→3:1に偏って固定される」現象について、`terraform plan`の出力に現れた属性名をきっかけに、AWS CLIで取得した実データ(停止タスク14件すべてが障害注入AZに集中、等)から自力で一貫した仮説を構築できた。新規知識であり、前回セッションからの持ち越し課題を解消した事例 |

## 曖昧なこと(知識の断片はあるが精度が甘い/ヒントがあれば気づける)

| 項目 | 状態 |
|---|---|
| Route53ヘルスチェックの適用範囲 | 単一リージョンでは不要、DR/マルチリージョン向けという整理に誘導で気づけた |
| 可観測性 | 監視・アラート・自動化の概念は持っているが、CloudWatch Logs Insights/X-Ray/Performance Insightsなど具体的な道具の実装経験がゼロ |
| リソース属性の「読み取り(Read)」と「設定(Write)」の区別 | `00-terraform-intro`で、Terraformがバケット作成後に行う多数の`Get`系API呼び出し(現状の読み取り)を「aclやタグの自動設定」と誤認した。似た見た目の動作でも実際の動詞(Get/List/Describe vs Put/Create/Update/Delete)で判断する視点が抜けていた(2026-07-11)。「似た機能の目的差分の混同」の新事例 |
| Fargateのターゲットタイプがip指定になる理由の説明、EIP(パブリックIP)とプライベートIPの区別 | 「ip」という正解自体は毎回出せるが、理由の説明で「タスクにEIPが割り当てられるから」という誤解が2026-07-12、2026-07-13と繰り返し出ている。正しくはプライベートIP(awsvpcモードの専用ENI)であり、EIP(固定パブリックIP)とは別物。EIPを学んだ直後の会話でも同じ誤りが再発しており、NAT GatewayのEIPとタスクのプライベートIPの区別が特に弱い。`06-multi-az-failure-simulation`の記録を見ない理解度チェック(2026-07-27)でも、ALBのヘルスチェック対象IPを「パブリックIP」と逆に説明したが、指摘後`assign_public_ip = false`の設定と即座に結びつけて自己修正できた。継続フォロー対象 |
| Web層など内部サーバーの外部通信プロトコル | NAT Gateway経由の外部通信(イメージpull等)がHTTPS/443であることを、HTTPと誤答したことがある(2026-07-12)。443番であるという知識は他の文脈(ALBのInboundなど)では正しく使えている。`04-ecs-iam-roles`のSG設計でも「443 or 80」と書き、確信が持てていなかった(2026-07-19)。本人も「まだわかっていない」と自覚しており、3回目以降の継続フォロー対象 |
| IAMロールの分類(責務)と、具体的なAPIアクションの内容は別の知識であるという区別 | `04-ecs-iam-roles`で、「Execution Role/Task Roleのどちらに割り当てるべきかが決まれば、具体的なAPIアクションも自明では」という主張があった。実際には、ECRプルとSecrets Manager参照は同じExecution Roleに分類されるが、必要なAPIアクションは全く異なる。「どのポリシー文書に書くか(ロール)」と「そのポリシーに何を書くか(アクション)」が別の設計判断であることの区別が抜けていた。指摘後は理解を修正できた(2026-07-19) |
| Terraformでのコピペ実装時の値の見直し漏れ、および実装時に「一文字単位でコードを見返す」精度 | 同じパターンのリソースを複製する際、ポート番号を一律コピペする、参照名やコンテナ名を直し忘れる、といったミスが`01-vpc-network`の実装で複数回発生。`03-rds-scaling`(SGルールのポート番号が`0`のまま、NACLルール番号の重複)、`05-ecs-autoscaling`(重複リソースラベル、`.id`の付け忘れ、コンテナ名のコピペ跡、修正箇所の取り違えなど9件中8件が同種)と4回連続で再発している(2026-07-13, 2026-07-16, 2026-07-23)。`05-ecs-autoscaling`の振り返りで本人が言語化した仮説: 「概念・設計が合っていることを確認した時点で注意力の大半を使い切ってしまい、実際にタイプ/編集した内容そのものを見返す一手間が抜けやすい」(本人の感覚レベルの分析)。対策として、休憩・ペース配分の工夫(疲れたら翌日に持ち越す)と、提出前に自分で読み返す習慣の両輪が次回の目標として合意された(2026-07-23)。`06-multi-az-failure-simulation`(2026-07-27)でも、`aws_ecs_service`の`network_configuration.subnets`が`aws_lb.alb`のサブネットを参照するミス(ALB用ブロックのコピペ跡)が5回目相当として再発したが、今回は指摘された瞬間に自分で「コピペ時の修正忘れ」と診断できており、`05`で合意した対策が機能し始めている兆しが見られた |
| 修正指示に対し、意図した箇所と異なる箇所に修正を適用してしまう | `05-ecs-autoscaling`のTerraform実装で2回発生した新しいミスパターン。IAMロールの信頼ポリシーを`locals`化する際、新しいブロックを追加したが古いブロックの削除を忘れ、かつ別のリソース(`aws_iam_role_policy.execution`)を誤って削除してしまった。Application Auto Scalingの`resource_id`の文字列補間修正でも、修正が`aws_appautoscaling_target`ではなく`aws_appautoscaling_policy`側に誤って適用された(2026-07-23) |
| `jsonencode`で書く生JSON(AWS API、camelCase)と、Terraform HCLの属性名(snake_case)の命名規則の混同 | `05-ecs-autoscaling`で、ECSタスク定義の`portMappings`内で`containerPort`と書くべきところを`container_port`(HCLの命名規則)と書いてしまい、AWS API側に認識されない状態になっていた(2026-07-23) |
| NACL/SGでステートレス/ステートフルの設計が分かれている理由(発展的内容) | 挙動の違い自体は完全に理解しているが、「なぜそう設計されているか」(サブネット境界の粗い防御=処理コスト優先でステートレス、ENI単位の細かい防御=柔軟性優先でステートフル)という設計思想は、理解度チェックで「わからない」と正直に申告(2026-07-13)。お題のスコープを超えた発展的な問いであり、優先度は低い |
| NACLの`protocol`フィールドに指定できる値の制約 | `03-rds-scaling`で、`protocol = "ssh"`のようにアプリケーション名を指定できると誤解していた。実際は`tcp`/`udp`/`icmp`などのプロトコル名(または番号)のみが有効で、SSHのようなアプリケーションはポート番号で区別するものであり、プロトコル自体の名前ではないという区別が抜けていた。同日の振り返り後の理解度チェックでは「NACLは第4層なのでDNSのようなアプリケーション層は理解できない」と正しく再現できたが、同一セッション内の再テストのため、日をまたいだ定着は次回以降で確認する(2026-07-16) |
| `for_each`で作ったリソース(マップ)から、単一要素を選ぶ/全要素を取り出す、それぞれの構文の使い分け | `03-rds-scaling`で、`aws_network_acl.db`の`subnet_ids`に`aws_subnet.db.id`(for_eachされたリソースへの誤った直接参照)と書いてしまった。`06-multi-az-failure-simulation`(2026-07-27)でも同種の誤り(`aws_subnet.ecs.id`とキー指定なしで参照)が再発したが、「わからない」と正直に表明した上で`aws_subnet.ecs["az2"].id`という単一要素参照の構文を一度の説明で習得し、直後に書いたALBの`subnets = [for s in aws_subnet.alb : s.id]`(全要素を取り出す構文)とは異なる目的の構文であることも正しく区別できていた |
| RDSリードレプリカ作成にソース側の自動バックアップ有効化(`backup_retention_period > 0`)が必須である理由 | `03-rds-scaling`のレビューで、設定自体の抜け(`backup_retention_period`未指定)を発見。振り返り後の理解度チェックで、必要な設定であること自体は覚えていたが理由は「わからない」と正直に申告したため、「レプリカ作成はソースの自動バックアップの仕組み(スナップショット+トランザクションログ)を土台にするため」という理由を伝えた。新規知識であり、自力で導出したものではない(2026-07-16) |
| Fargateタスク起動時の「ENI付与→イメージプル→コンテナ起動」という順序の理解 | `05-ecs-autoscaling`で、イメージプルの通信要件を説明する際に「コンテナが存在→ENI付与→イメージプル」という逆順で語っていた。最終的な結論(Outbound 443の必要性)自体は正しかったが、それを支える内部の順序の理解が逆転していた「答えは合っているが根拠となる仕組みの理解が誤っている」パターンの一例(2026-07-23) |

## ほぼ理解できていないこと

(現時点で「ほぼ理解できていない」と判定するほどの項目はなし。`03-rds-scaling`でRDS Multi-AZとリードレプリカの目的差分を検証済みで、上記「理解できていること」に格上げした)

## 学習傾向の分析(重要)

弱点には共通パターンがある: **「似た見た目の機能の目的の違い」を混同しやすい**(NACL vs SG、Multi-AZ vs リードレプリカ、Task Role vs Execution Role、EIP vs プライベートIP)。機能の存在は知っているが「何のためにあるか」の解像度が粗い。**今後の出題は、この「目的差分」を突く設計にすると弱点を効率よく可視化できる。**

2026-07-13の`01-vpc-network`Terraformレビューで新たに見えた傾向: **「同一セッション内で一度説明を受けて理解した」ことと「後のテストで再現できる」ことの間にギャップがある。** Execution Role/Task Roleのログ書き込み担当、Fargateのip指定の理由は、いずれも当日中に説明済みにもかかわらず、直後の理解度テストで再度誤答した。これは知識が「その場では納得したが、まだ自分の言葉の中に定着していない」状態を示しており、単発の説明では不十分で、間隔を空けた復習(スペースドリピティション的な再確認)が必要な項目だと考えられる。次回以降のお題でも、この2点は継続してフォローする。

一方で強み: ソクラテス式のヒントを与えると自力で正解に到達できる(NAT Gateway/IGWの例、ECSクラスターの役割の例、`02-nacl-experiment`でのSG Denyルール不在への気づき)。初見の問題に対して筋道立てて考える力があり、「暗記した手順をなぞるだけ」ではない。IAM/S3のARN粒度への理解は同年次の中でも精度が高い。また、指摘に対して率直に「暗記知識だった」「整理できていなかった」「わからない」と認める自己認識の正直さがあり、これは学習効率を高める良い資質。

2026-07-13の`02-nacl-experiment`で新たに見えた傾向: **実機トラブルシューティングで得た教訓が、直後に自分でゼロから実装する場面へ自動的に転用されるとは限らない。** AWSコンソール構築時に「インターネット経路がなくyum installが失敗する」という問題を自力で解決した直後にもかかわらず、その後Terraformでセキュリティグループを書く際には同種の見落とし(DNS/HTTP(S)へのEgress許可漏れ)を再度犯した。「一度デバッグして解決した」ことと「次に自分で設計する際にチェックリストとして能動的に想起できる」ことは別のスキルであり、実装課題では都度「起動スクリプトが必要とする副次的な通信は洗い出したか」を意識的に問いかけるとよい。

2026-07-16の`03-rds-scaling`で新たに見えた強み: **メンター(Claude)の指摘に対しても鵜呑みにせず、事実(実機での動作確認結果)と論理(ポート範囲の数値的包含関係)の両方を根拠に反論し、実際にメンター側の誤りを正すことができた。** `02-nacl-experiment`で見られた「根拠の強さを自分で吟味する姿勢」がさらに発展し、抽象的な数値関係の理解と実地検証を組み合わせて批判的に検証する力として現れている。一方で、`for_each`の判断基準や「コピペ実装時の値の見直し漏れ」など、既存の弱点パターンは引き続き複数回の指摘を要しており、強みと弱点が同じセッション内で両方はっきり見える回だった。

2026-07-16の振り返り後に実施した記録を見ない理解度チェック(10問)で、これまで課題とされてきた「同一セッション内で理解しても後のテストで再現できない」というパターンに、**日をまたいだ定着が確認できた反例**が出た。2026-07-14の構築時理解度チェックで誤答・部分理解だった3項目(リードレプリカが非同期な理由、Multi-AZフェイルオーバー時のDNS切り替えの仕組み、SG自己参照ルールの送信側/受信側の役割分担)が、2日後の今回はすべて正確に再現できた。これは、実機構築という体験を伴った学習が、単なる口頭説明よりも定着しやすい可能性を示唆しており、今後もトラブルシューティングや実機確認を伴う課題設計を優先する根拠になる。

一方で、メンター(Claude)側の推測に対しても鵜呑みにせず、実機の再現テストで反証するという批判的思考も`02-nacl-experiment`で見られた(「curl失敗は古いIPを使ったからでは」という推測に対し、正しいIPでも1回失敗した実例を示して指摘)。指示に従うだけでなく、根拠の強さを自分で吟味する姿勢は今後も伸ばしていきたい。

2026-07-19の`04-ecs-iam-roles`で新たに見えた強み: **設計フェーズでの指摘を、実装フェーズに再指摘なしで転移させる力。** 設計レビュー時に「IAMポリシーのResourceをハードコードせずリソース参照にする」という改善点を指摘したところ、Terraform実装フェーズ(数日後)で、指摘を再度受けることなく全箇所に一貫して適用できた。`02-nacl-experiment`で見られた「実機デバッグの教訓が新規実装に自動転用されるとは限らない」という過去の課題とは対照的に、今回は文書化されたフィードバック(レビュー結果)を後工程に活かせている。また、Docker Desktopの起動不能という想定外の環境トラブルに対し、根本解決(WSL修復)に固執せず、AWS CloudShellへの切り替えという実務的な代替手段を即座に選択できた点も、実務対応力として評価できる。

一方で、お題の核心(意図的な権限剥奪と実機エラーからの切り分け)は達成できたものの、本人自身が「自分で変更した箇所なので切り分けの自明性は高かった」と正直に振り返っている。第三者が仕込んだ障害を切り分けるような、より曖昧な状況での切り分け力は今後の課題として残る。

2026-07-23の`05-ecs-autoscaling`Terraformレビュー・振り返りで新たに見えた強み: **判断基準そのものを新しい題材に自力で転移させる力が一段進んだ。** `04-ecs-iam-roles`で見られた「文書化されたフィードバックの後工程への転移」に続き、今回は`03-rds-scaling`で身につけた`for_each`の判断基準(役割ベース)を、指摘される前に自分から「ALBサブネットは役割が同じだから`for_each`にすべきでは」と提起し、さらに`variable`/`locals`の使い分けも一度の説明でIAMトラストポリシーの共通化まで自発的に実装した。設計・概念理解の面でのつまずきは今回ゼロだった。

一方で、実装のミス(9件中8件)は「概念・設計の正しさ」とは別の層、すなわち「実際にタイプ/編集したコードを一文字単位で見返す精度」に集中して発生した。振り返りの対話を通じて本人が「概念・設計を確認した時点で注意力の大半を使い切ってしまい、見返す一手間が抜けやすい」という仮説を自ら言語化できたことは、`01`から4回続く「コピペ実装時の値の見直し漏れ」パターンに対する初めての具体的な原因仮説であり、[[feedback_session_pacing_and_wellbeing]]で継続的に見られてきた疲労傾向とも接続する可能性がある。対策として休憩・ペース配分の工夫と提出前の読み返し習慣の両輪が本人の意向として合意された。次回以降、この対策の効果(実装ミスの頻度変化)を観察する。

振り返り完了後、本人の希望により記録を見ない理解度チェック(10問)を追加実施(2026-07-23、同一セッション内)。スケーリングポリシーの選定理由、登録解除の遅延、SG-ENIの関係、`for_each`/`variable`/`locals`の判断基準、Fargateタスク起動順序(ENI→プル→起動。セッション内で一度訂正した点)、ECR利用時のIAM権限の構造、`aws_appautoscaling_target`/`policy`の役割分担、単一AZ設計のコストトレードオフ、ヘルスチェック閾値のトレードオフを出題し、10問中10問を正確に回答できた。10問目では設計書に明記していなかった追加の視点(異常閾値が低すぎる場合、負荷で応答が遅いだけの正常なリソースを誤ってダウン判定するリスク)まで自発的に言及した。7問目(ECRの具体的なAPIアクション名)は「忘れたが調べればわかる」と正直に申告した。ただし同一セッション内での即時確認のため、日をまたいだ定着は次回以降で確認する。

2026-07-27の`06-multi-az-failure-simulation`のTerraform実装・振り返りで新たに見えた強み: **原因不明に見える現象でも、対話を通じて手持ちのデータから一貫した仮説を組み立てる粘り強さ。** AWSコンソール構築時に「原因未解明」のまま持ち越されていた「AZタスク配置の偏り」について、当初は「ログだけでは推測できない」と結論を急いだが、`terraform plan`に現れた属性名(`availability_zone_rebalancing`)を手がかりに、AWS CLIで取得した実データから自力で仮説を構築できた。一方で、「わからない」と判断する前に、手持ちのデータをもう一段掘る余地があったことも本人が振り返りで認めており、結論を急ぐ傾向は今後も意識すべき点として残る。

また、`05-ecs-autoscaling`の振り返りで合意した「コピペ後に読み返す習慣」が、`06`で発生したコピペミス(ECSサービスのサブネット参照)に対して、指摘された瞬間に自分で「コピペ時の修正忘れ」と診断できるという形で、初めて機能し始めた兆しが見られた。ミス自体はまだ発生している(5回目相当)が、「指摘されるまで気づけない」から「指摘されれば即座に自己診断できる」への変化は、対策が完全に定着する前段階として評価できる。

## 更新履歴

- 2026-07-11: 初回評価を実施しこのファイルを作成
- 2026-07-11: `00-terraform-intro`のレビュー結果を反映。Terraform基礎フローと障害切り分け力を格上げ、「読み取りと設定の混同」「tfstateの機密情報リスクの実感不足」を新たに曖昧な項目として追加(詳細は[projects/00-terraform-intro/RETROSPECTIVE.md](../projects/00-terraform-intro/RETROSPECTIVE.md))
- 2026-07-13: `01-vpc-network`の振り返りを反映。NAT Gateway/IGW、EIP、ALB用語、ECSクラスター、Auto Scalingとタスクの関係、RDS Multi-AZの目的、tfstateと変数の違いを格上げ。ECS Execution/Task Roleのログ担当とFargateのip指定理由は、同一セッション内での再誤答が確認されたため「曖昧」に留め置き、継続フォロー対象とした。新たに「コピペ実装時の値の見直し漏れ」という実装スキル面の傾向を追加(詳細は[projects/01-vpc-network/RETROSPECTIVE.md](../projects/01-vpc-network/RETROSPECTIVE.md))
- 2026-07-13: `02-nacl-experiment`の振り返りを反映。NACLのステートレス挙動・エフェメラルポート、SG/NACLの許可拒否モデルの違い、SGのステートフル性が効く範囲を格上げ(記録を見ない理解度チェックで確認)。Terraformの`variable`/`locals`/`for_each`の使い分けと、NACL/SGの設計思想の「なぜ」を新たに曖昧な項目として追加。「実機デバッグの教訓が新規実装に自動転用されない」という学習傾向を新たに追加(詳細は[projects/02-nacl-experiment/RETROSPECTIVE.md](../projects/02-nacl-experiment/RETROSPECTIVE.md))
- 2026-07-16: `03-rds-scaling`の振り返りを反映。RDS Multi-AZとリードレプリカの目的差分を「ほぼ理解できていない」から格上げ(解消)。`for_each`の判断基準(役割ベース)、SGの`from_port`/`to_port`の意味、ポート範囲の数値的包含関係の理解を新たに格上げ。NACLの`protocol`フィールドの制約、`for_each`リソースからの値抽出構文を新たに曖昧な項目として追加。「コピペ実装時の値の見直し漏れ」が3回目の再発であることを記録。「メンターの誤った指摘を事実と論理で反論できる」という新たな強みを追加(詳細は[projects/03-rds-scaling/RETROSPECTIVE.md](../projects/03-rds-scaling/RETROSPECTIVE.md))
- 2026-07-16: 振り返り後、記録を見ない理解度チェック(10問)を追加実施。2026-07-14の構築時に誤答・部分理解だった3項目(リードレプリカが非同期な理由、Multi-AZフェイルオーバー時のDNS仕組み、SG自己参照ルールの送信側/受信側の役割分担)が、2日後の今回すべて正確に再現でき、「理解できていること」に格上げ。RDSリードレプリカのバックアップ要件の理由(新規知識)を追加
- 2026-07-19: `04-ecs-iam-roles`の振り返りを反映。ECS Task Execution Role/Task Roleの責務分担を「曖昧」から格上げ(`01-vpc-network`での2回連続誤答から解消。実機のエラーメッセージからの切り分けまで達成)。「IAMロールの分類とAPIアクションの内容は別の知識」という新たな曖昧項目を追加。HTTP/HTTPSの区別・`variable`/`locals`の使い分けは、今回も継続フォローのまま(前者は本人が明確に「まだわかっていない」と自覚)。「設計フェーズの指摘を実装フェーズに転移させる力」「予期しない環境トラブルへの実務的な代替対応力」を新たな強みとして追加(詳細は[projects/04-ecs-iam-roles/RETROSPECTIVE.md](../projects/04-ecs-iam-roles/RETROSPECTIVE.md))
- 2026-07-23: `05-ecs-autoscaling`の振り返りを反映。`for_each`の判断基準の新題材への自力転用、`variable`/`locals`の使い分け(`02`〜`04`から継続フォロー中だった項目)、ECS Agentとタスクのネットワークの関係を格上げ。新たな曖昧項目として「修正箇所の取り違え」「`jsonencode`内camelCaseとHCL属性名snake_caseの混同」「Fargateタスク起動順序(ENI→プル→起動)の理解」を追加。「コピペ実装時の値の見直し漏れ」(4回目の再発)について、本人が振り返りの対話で「概念・設計確認後に注意力を使い切り、見返す一手間が抜ける」という具体的な原因仮説を初めて言語化し、休憩・ペース配分と読み返し習慣という対策に合意した。「判断基準そのものを新しい題材に自力で転移させる力」を新たな強みとして追加(詳細は[projects/05-ecs-autoscaling/RETROSPECTIVE.md](../projects/05-ecs-autoscaling/RETROSPECTIVE.md))
- 2026-07-27: `06-multi-az-failure-simulation`の設計レビュー(07-25)〜Terraform実装・振り返り(07-27)を反映。ALB/ECSサービス/ECSエージェント/Auto Scalingの役割分担、AZ障害注入実験の設計・実施・観察能力を格上げ。ECSのAZ Rebalancing機能(`availability_zone_rebalancing`)の理解を新規知識として追加(前回セッションで「原因未解明」だった論点を自力調査で解消)。EIP/プライベートIPの混同、`for_each`の単一要素参照構文、コピペ実装時の見直し漏れは、いずれも継続項目として再発を記録しつつ、指摘後の即時自己修正・自己診断ができた点を進歩として明記。「結論を急ぐ前にデータをもう一段掘る」という新たな成長課題を追加(詳細は[projects/06-multi-az-failure-simulation/RETROSPECTIVE.md](../projects/06-multi-az-failure-simulation/RETROSPECTIVE.md))
